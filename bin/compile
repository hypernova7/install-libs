#!/usr/bin/env bash

if [ -z "$SOURCE_VERSION" ]; then
  echo "Not found SOURCE_VERSION"
  exit 0
fi

export "RELEASE=$SOURCE_VERSION"
export "SOURCE_VERSION=$SOURCE_VERSION"
export "BUILD_DIR=$1"
# echo "RELEASE=$SOURCE_VERSION" >> $1/.env
# echo "SOURCE_VERSION=$SOURCE_VERSION" >> $1/.env
