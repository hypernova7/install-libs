#!/bin/sh

SRC=$1/.env
VERSION=2:4:6

echo "RELEASE=$SOURCE_VERSION" >> $SRC
echo "SOURCE_VERSION=$SOURCE_VERSION" >> $SRC

echo $VERSION | awk 'BEGIN { FS=":" } { $3++;  if ($3 > 99) { $3=0; $2++; if ($2 > 99) { $2=0; $1++ } } } { printf "%02d:%02d:%02d\n", $1, $2, $3 }'
echo $VERSION
